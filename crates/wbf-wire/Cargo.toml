[package]
name = "wbf-wire"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "wbfuwunel 線上協議的 codec：pack、EncryptedFileInfo、CRC-32C。純函數，無 async。"

[dependencies]
crc32c = "0.6"

[dev-dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
